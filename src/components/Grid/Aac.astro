---
---

<div class="wrapper">
  <strong>AAC</strong>
  <span>Your goto text-to-speech companion</span>
</div>

<style lang="scss">
  div.wrapper {
    width: 100%;
    height: 100%;
    border-radius: 6px;
    padding: 16px;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    border: 1px solid var(--colour-border);
    background-color: var(--colour-foreground);
    strong {
      margin: 0;
      margin: 0;
      font-size: 32px;
      line-height: 32px;
      font-weight: 700;
      position: relative;
      display: block;
      transform: translate(calc(var(--x) * -4px), calc(var(--y) * 4px));
      margin-bottom: 4px;
    }
    span {
      margin: 0;
      font-size: 16px;
      font-weight: 500;
      opacity: 0.6;
      display: block;
      transform: translate(calc(var(--x) * -3px), calc(var(--y) * 3px));
    }
  }
</style>
